apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounts
  labels:
    app: accounts
spec:
    replicas: 3
    selector:
      matchLabels:
        app: accounts-pod
    template:
      metadata:
        labels:
          app: accounts-pod
          project: banking
      spec:
        containers:
        - name: accounts
          image: bilal690/banking-accounts:0.3.develop
          ports:
          - containerPort: 3000
          startupProbe:
            httpGet:
              path: /healthz
              port: 3000
            failureThreshold: 5
            periodSeconds: 3
          env:
            - name: CUSTOMER_SERVICE_URL
              value: http://develop-customer-service
            - name: TRANSACTIONS_SERVICE_URL
              value: http://develop-transactions-service
            - name: TESTDATA_SERVICE_URL
              value: http://develop-testdata-service