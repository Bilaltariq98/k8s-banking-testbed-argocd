apiVersion: apps/v1
kind: Deployment
metadata:
  name: testdata
spec:
  template:
    spec:
      containers:
        - name: testdata
          image: k3d-banking-registry/banking-testdata:local
          ports:
          - containerPort: 3000
