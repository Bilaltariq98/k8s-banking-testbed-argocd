apiVersion: apps/v1
kind: Deployment
metadata:
  name: testdata
spec:
  template:
    spec:
      containers:
        - name: testdata
          image: bilal690/banking-testdata:0.1.develop
          ports:
          - containerPort: 3000
