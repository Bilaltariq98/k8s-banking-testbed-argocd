apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: testdata
  labels:
    app: testdata
spec:
  template:
    spec:
      containers:
        - name: testdata
          image: ${REGISTRY}/${REPOSITORY}:${IMAGE_TAG}
          ports:
            - containerPort: 3000
