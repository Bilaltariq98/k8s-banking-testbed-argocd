apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: tests
  namespace: operations
  labels:
    app: tests
spec:
    template:
      spec:
        containers:
        - name: tests
          image: k3d-banking-registry/banking-tests:local
          ports:
          - containerPort: 3000