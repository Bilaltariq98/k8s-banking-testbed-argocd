apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: tests
  namespace: operations
  labels:
    app: tests
spec:
    template:
      spec:
        containers:
        - name: tests
          image: bilal690/banking-tests:0.4.develop
          ports:
          - containerPort: 3000